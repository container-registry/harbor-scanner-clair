---
apiVersion: v1
kind: Service
metadata:
  name: harbor-scanner-clair
spec:
  selector:
    app: harbor-scanner-clair
  type: LoadBalancer
  ports:
    - name: api-server
      protocol: TCP
      port: 8443
      targetPort: 8443
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: harbor-scanner-clair
spec:
  replicas: 1
  selector:
    matchLabels:
      app: harbor-scanner-clair
  template:
    metadata:
      labels:
        app: harbor-scanner-clair
    spec:
      containers:
        - name: main
          image: 8gears.container-registry.com/8gcr/harbor-scanner-clair:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: "SCANNER_LOG_LEVEL"
              value: "info"
            - name: "SCANNER_API_SERVER_ADDR"
              value: ":8443"
            - name: "SCANNER_API_SERVER_TLS_CERTIFICATE"
              value: "/certs/tls.crt"
            - name: "SCANNER_API_SERVER_TLS_KEY"
              value: "/certs/tls.key"
            - name: "SCANNER_API_SERVER_READ_TIMEOUT"
              value: "15s"
            - name: "SCANNER_API_SERVER_WRITE_TIMEOUT"
              value: "15s"
            - name: "SCANNER_CLAIR_URL"
              value: "http://harbor-harbor-clair:6060"
            # The DSN is a credential, so it comes from a Secret rather than
            # from this manifest. The adapter rejects the removed
            # SCANNER_STORE_REDIS_* names at startup.
            - name: "SCANNER_STORE_POSTGRES_URL"
              valueFrom:
                secretKeyRef:
                  name: harbor-scanner-clair-postgres
                  key: url
            - name: "SCANNER_STORE_SCAN_JOB_TTL"
              value: "1h"
            - name: "SCANNER_TLS_INSECURE_SKIP_VERIFY"
              value: "false"
            - name: "SCANNER_TLS_CLIENTCAS"
              value: ""
          ports:
            - name: api-server
              containerPort: 8443
          livenessProbe:
            httpGet:
              scheme: HTTPS
              port: api-server
              path: /probe/healthy
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 10
          readinessProbe:
            httpGet:
              scheme: HTTPS
              port: api-server
              path: /probe/ready
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          volumeMounts:
            - mountPath: /certs
              name: certs
              readOnly: true
      volumes:
        - name: certs
          secret:
            secretName: harbor-scanner-clair-tls
