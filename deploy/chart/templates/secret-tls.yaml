{{- if and .Values.api.tls.enabled (not .Values.api.tls.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-tls" (include "harbor-scanner-clair.fullname" .) }}
  namespace: {{ include "harbor-scanner-clair.namespace" . }}
  labels:
    {{- include "harbor-scanner-clair.labels" . | nindent 4 }}
  {{- with (include "harbor-scanner-clair.annotations" (dict "root" . "local" dict)) }}
  annotations:
    {{- . | trim | nindent 4 }}
  {{- end }}
type: kubernetes.io/tls
data:
  tls.crt: {{ .Values.api.tls.certificate | b64enc | quote }}
  tls.key: {{ .Values.api.tls.key | b64enc | quote }}
{{- end }}
